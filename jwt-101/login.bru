meta {
  name: login
  type: http
  seq: 4
}

post {
  url: http://localhost:3000/login
  body: json
  auth: inherit
}

body:json {
  {
    "username": "test2",
    "password": "aa"
  }
}

script:post-response {
  // Stores the accessToken in an environment or global variable
  var accessToken = res.getBody().token;
  bru.setEnvVar("jwt101", accessToken);
}

settings {
  encodeUrl: true
  timeout: 0
}
